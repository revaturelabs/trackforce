export class GraphCounts {
  id: number;
  name: string;
  count: number;

  
  constructor(id?: number, name?: string, count?: number) {
    this.id = id
    this.name = name
    this.count = count
  }
}

